# Versions tested together on Python 3.11 (see README for setup).
# Install torch FIRST from the right index (scripts/setup.* do this automatically):
#   NVIDIA GPU: pip install torch==2.14.0 torchvision==0.29.0 --index-url https://download.pytorch.org/whl/cu130
#   CPU only:   pip install torch==2.14.0 torchvision==0.29.0 --index-url https://download.pytorch.org/whl/cpu
# (On Windows the plain PyPI torch wheel is CPU-only.)

# API
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
python-dotenv==1.2.3
python-multipart==0.0.32

# Reasoning layer (Groq / OpenRouter / Together / Ollama via the OpenAI-compatible API; Anthropic optional)
anthropic==1.8.0
openai==3.19.2

# Vision execution heads (training + inference)
torch==2.14.0
torchvision==0.29.0
segmentation-models-pytorch==0.5.0

# Geospatial + raster
rasterio==1.4.4
numpy==2.4.6
opencv-python-headless==5.0.0.93
pillow==12.3.0
scikit-image==0.26.0
geopandas==1.1.4
shapely==2.1.2
pyproj==3.7.2
requests==2.34.2
tqdm==4.70.1

# Reports
reportlab==5.0.1
matplotlib==3.11.2

# Tests
pytest==9.1.1
httpx==0.28.1
earthengine-api==1.7.45
